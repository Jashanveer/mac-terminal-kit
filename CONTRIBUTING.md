# Contributing

Keep changes compatible with the macOS system Bash and zsh. Run
`./scripts/lint.sh` and `./tests/run.sh` before opening a pull request. Changes
to installation behavior must remain idempotent and preserve unrelated user
configuration.
